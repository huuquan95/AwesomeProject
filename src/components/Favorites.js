
import React, { Component } from 'react';
import {
    Platform,
    StyleSheet,
    Text,
    View, Image
} from 'react-native';

export default class Favorites extends Component {

    state = {}
    render() {
        return (
            <Text>Favorites</Text>
        );
    }
}